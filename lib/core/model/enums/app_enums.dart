enum AppRoleEnum { student, teacher, user }

enum CourseStateEnum { active, completed, suspended }

enum AppLanguageEnum { arabic, english }

enum AppStateEnum { user, guest }

enum AppThemeEnum { light, dark }

enum ChapterRowStateUserEnum { guest, userPaid, userUnpaid }

enum ChapterStateEnum { open, locked }

enum CourseStatusEnum { active, notActive }

enum RefreshStatusEnum {
  initial,
  success,
  failure,
}

enum ResponseStatusEnum {
  initial,
  loading,
  success,
  failure,
}
